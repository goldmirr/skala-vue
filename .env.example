# 이 파일을 복사해서 .env 로 이름을 바꾸고 실제 키를 넣으세요 (cp .env.example .env)
# Vite 에서 코드로 읽으려면 변수 이름이 반드시 VITE_ 로 시작해야 합니다
# 키 발급: https://home.openweathermap.org/api_keys (가입 후 무료 발급, 활성화까지 수십 분 걸릴 수 있음)
VITE_OPENWEATHER_API_KEY=여기에_발급받은_API_키를_넣으세요
